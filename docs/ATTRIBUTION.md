# Incident-scoped attribution

Attribution is downstream of incident confirmation. It ranks observable cohort evidence using excess failed value, economic contribution, effect size, sample size, and limited two-way interactions. Attribution cannot veto or alter detector state.
